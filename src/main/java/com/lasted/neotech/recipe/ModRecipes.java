package com.lasted.neotech.recipe;

import net.neoforged.bus.api.IEventBus;

// Placeholder for future formal recipe registrations if needed.
public class ModRecipes {
    public static void register(IEventBus bus) {
        // No-op: using a custom reload listener (PortableMiningManager) instead of vanilla Recipe API.
    }
}
